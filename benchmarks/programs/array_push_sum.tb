int main(string[] args):
    int[] values = []
    int seed = args.length()
    for (int i = 0; i < 30000; i++):
        values.push((i * 31 + seed) % 997)

    int total = 0
    for (int i = 0; i < values.length(); i++):
        total += values[i] * (i % 13)

    print(total)
    return 0
