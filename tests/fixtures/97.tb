string fail_label(string text):
    throw 'no:' + text

int main(string[] args):
    thread_pool pool = thread_pool(2)
    future<string> result = pool.submit(fail_label, 'work')
    try:
        print(result.await())
    catch (exception err):
        print(err)
    return 0
