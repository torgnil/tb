int main(string[] args) {
    int[] my_array = [1, 3, 2];
    prio_q<int> pq = my_array.create_prio_q((int a, int b) -> b - a);
    pq.push(4);
    my_array.push(5);
    while (!pq.is_empty()) {
        print(pq.pop());
    }
}