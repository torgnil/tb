bool is_even(int x):
    return x % 2 == 0

int main(string[] args):
    thread_pool pool = thread_pool(2)
    future<bool> a = pool.submit(is_even, 12)
    future<bool> b = pool.submit(is_even, 7)
    print(a.await())
    print(b.await())
    return 0
