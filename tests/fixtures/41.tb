int main(string[] args) {
    string[] cli = ['tb', '--help', '--out=result.ll', '--input', 'main.tb'];
    print(has_flag(cli, '--help'));
    print(option_value(cli, '--out'));
    print(option_value(cli, '--input'));
    print(option_value(cli, '--missing'));
    return 0;
}
