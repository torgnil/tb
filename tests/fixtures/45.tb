record Point(int x, int y);

Point make_point(int x, int y) {
    return Point(x, y);
}

int main(string[] args) {
    Point point = make_point(7, 9);
    print(point.x);
    print(point.y);
    print(point.x + point.y);
    return 0;
}
