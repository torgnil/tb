int compare_down(int left, int right):
    return right - left

int main(string[] args):
    int[] values = [1, 3, 2]
    prio_q<int> pq = values.create_prio_q(compare_down)
    pq.push(4)
    values.push(5)
    while (!pq.is_empty()):
        print(pq.pop())
    return 0
