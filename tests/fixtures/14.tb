int main(string[] args) {
    string s = '1,2, 3';
    string split[] = s.split(',');
    int sum = 0;
    for (string part : split) {
        part = part.trim();
        sum += part.to_int();
        print(part);
    }
    print(sum);
    return 0;
}
