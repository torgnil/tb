int fail_task(int x):
    throw 'bad=' + x

int main(string[] args):
    thread_pool pool = thread_pool(2)
    future<int> result = pool.submit(fail_task, 7)
    try:
        print(result.await())
    catch (exception err):
        print(err)
    return 0
