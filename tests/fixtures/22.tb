int main(string[] args) {
    int[] numbers = [3, 4];
    numbers.push(5);
    numbers[1] = numbers[1] + numbers[2];
    for (int value : numbers) {
        print(value);
    }
    print(length(numbers));
    return 0;
}
