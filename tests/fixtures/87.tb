int square(int x):
    return x * x

future<int> schedule_square(thread_pool pool, int x):
    return pool.submit(square, x)

int main(string[] args):
    thread_pool pool = thread_pool(2)
    future<int> result = schedule_square(pool, 9)
    print(result.await())
    return 0
