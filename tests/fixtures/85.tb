int main(string[] args):
    map<int, int> counts = {0: 1}
    int key = 1
    counts[key] = 3
    counts[key] = counts[key] + 4
    counts[key] += 5
    counts[0] = counts[0] + key
    print(counts[key])
    print(counts[0])
    return 0
