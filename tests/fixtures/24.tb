int main(string[] args) {
    string text = read_file('test-files/24.in');
    string[] lines = text.split('
');
    string first = lines[0];
    string[] parts = first.split(',');
    print(parts[0]);
    print(parts[1]);
    print(first.length());
    return 0;
}
