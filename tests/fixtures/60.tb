record Grid(int[][] rows);

cached int score(Grid grid) {
    return (grid.rows[0][1] << 2) + popcount(grid.rows[1][0] >> 1) + popcount(grid.rows[1][1]);
}

int main(string[] args) {
    Grid grid = Grid([[1, 3], [14, 7]]);
    print(score(grid));
    print(score(grid));
}
