record Range(int start, int end);

int main(string[] args) {
    Range current = Range(2, 4);
    current.end = current.end + 3;

    print(current.start);
    print(current.end);

    Range[] ranges = [current];
    print(ranges[0].start);
    print(ranges[0].end);
    return 0;
}
