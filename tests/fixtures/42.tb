int main(string[] args) {
    set<int> s = {1, 2, 3};
    print(s.length());
    s.add(4);
    s.add(2);
    print(s.length());
    s = s.union({3, 4, 5});
    print(s);
    int[] arr = [1, 2, 3, 3];
    set<int> s2 = to_set(arr);
    print(s2);
    for (int x : s2) {
        print(x);
    }
    return 0;
}
